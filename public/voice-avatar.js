/**
 * Legacy voice guide injector — disabled. Voice guide lives in React (`VoiceAvatar` in main.tsx).
 * Kept so old cached index.html that still references this URL loads safely.
 */
(function () {
  "use strict";
})();
